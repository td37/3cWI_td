package at.td.ooEinstieg.musicbox;

public class Player {
    private boolean loaded;

    public Player(boolean loaded) {
        this.loaded = loaded;
    }

    }
    public void play(){
        if this.magazine.loadRecord()
        System.out.println(this.magazine.getRecords().get(this.magazine.loadRecord()));;
    }

    public boolean isLoaded() {
        return loaded;
    }

    public void setLoaded(boolean loaded) {
        this.loaded = loaded;
    }
}
